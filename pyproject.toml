[project]
name = "voice-interview-assistant"
version = "0.1.0"
description = "Real-time voice interview assistant with knowledge-grounded responses"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
